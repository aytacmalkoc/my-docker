-- MySQL başlangıç scripti
-- Bu dosya container ilk oluşturulduğunda otomatik olarak çalıştırılır

-- Örnek veritabanları oluştur (isteğe bağlı)
-- CREATE DATABASE IF NOT EXISTS php54_db;
-- CREATE DATABASE IF NOT EXISTS php7_db;
-- CREATE DATABASE IF NOT EXISTS php8_db;

-- Kullanıcı yetkilerini ayarla (docker-compose.yml'de zaten yapılandırılmış)
-- GRANT ALL PRIVILEGES ON *.* TO 'dev_user'@'%';
-- FLUSH PRIVILEGES;

-- Bu dosyayı ihtiyacınıza göre özelleştirebilirsiniz

